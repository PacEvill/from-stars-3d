# String de conexão para o banco de dados.
# Substitua pelos dados do seu banco de dados local ou de desenvolvimento.
# Formato para PostgreSQL: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Formato para SQLite (exemplo): file:./dev.db
DATABASE_URL="postgresql://user:password@localhost:5432/fromstars3d?schema=public"

# URL base do site. Usada para metadados de SEO e outras URLs canônicas.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
